{
  "annotations": {"list": []},
  "editable": false,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "reqps"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "targets": [{"expr": "sum(rate(route_search_total[$__rate_interval]))", "legendFormat": "searches/s", "refId": "A"}],
      "title": "Route search throughput",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "percentunit", "min": 0, "max": 1}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [{"expr": "1 - (sum(rate(http_server_requests_total{route=~\"/api/.*\",status_code=~\"5..\"}[$__rate_interval])) / clamp_min(sum(rate(http_server_requests_total{route=~\"/api/.*\"}[$__rate_interval])), 0.000001))", "legendFormat": "availability", "refId": "A"}],
      "title": "API availability",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "percentunit", "min": 0, "max": 1}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 8},
      "id": 3,
      "targets": [{"expr": "sum(rate(green_route_found_total[$__rate_interval])) / clamp_min(sum(rate(route_search_total[$__rate_interval])), 0.000001)", "refId": "A"}],
      "title": "Green route found ratio",
      "type": "gauge"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "s"}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 8},
      "id": 4,
      "targets": [{"expr": "histogram_quantile(0.95, sum by (le) (rate(initial_candidates_duration_seconds_bucket[$__rate_interval])))", "legendFormat": "initial p95", "refId": "A"}, {"expr": "histogram_quantile(0.95, sum by (le) (rate(enhanced_search_duration_seconds_bucket[$__rate_interval])))", "legendFormat": "enhanced p95", "refId": "B"}],
      "title": "Search latency objectives",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "short"}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 8},
      "id": 5,
      "targets": [{"expr": "sum(increase(route_search_total[$__range])) by (status)", "legendFormat": "{{status}}", "refId": "A"}],
      "title": "Search outcomes in range",
      "type": "piechart"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 41,
  "tags": ["greenroute", "product"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "timezone": "browser",
  "title": "GreenRoute — Product Overview",
  "uid": "greenroute-product-overview",
  "version": 1
}
